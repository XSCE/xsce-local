#!/bin/bash
# part of XSCE 
# script to turn off the GUI
if [ `id -u` -ne 0 ]; then
  echo "please become root to execute this command"
  exit 1
fi
systemctl disable olpc-dm.service
systemctl stop olpc-dm.service
read -p "This command requires a reboot. Use <ctl>C to abort the reboot" ans
reboot
